mixin parseObject(obj)
  each child in obj
    if child !== null && typeof child.children === 'object'
      | #[#{child.name}&attributes(child.attribs) #[+parseObject(child.children)]]
    else if child !== null && child.type === 'text'
      | #{child.data}
